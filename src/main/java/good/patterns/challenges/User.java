package good.patterns.challenges;

import good.patterns.challenges.abstractClasses.AbstractUser;

public class User extends AbstractUser {
    public User(String userName, String mail) {
        super(userName, mail);
    }
}
